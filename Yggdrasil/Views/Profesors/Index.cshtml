@model IEnumerable<Yggdrasil.Models.DBModels.Profesor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameProfesor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnrollDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameProfesor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnrollDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProfesor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProfesor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProfesor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
